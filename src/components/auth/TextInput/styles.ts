import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    field: {
        marginBottom: 8,

        position: 'relative',
    },
    label: {
        fontWeight: '700',
    },
    input: {
        padding: 8,
        flexGrow: 1,
    },
    icon: {
        padding: 8,
    },
    inputContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        borderRadius: 4,
        overflow: 'hidden',

    },
})